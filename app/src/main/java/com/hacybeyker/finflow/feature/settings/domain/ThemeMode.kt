package com.hacybeyker.finflow.feature.settings.domain

/** How the app resolves light/dark. [SYSTEM] follows the device setting; the others force one. */
enum class ThemeMode { SYSTEM, LIGHT, DARK }
